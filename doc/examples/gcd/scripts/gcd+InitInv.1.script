substitute on='let $mod := $everything :: let $decr := 0 :: let r := a :: let x := b :: r > 0';
substitute on='let $decr := 0 :: let r := a :: let x := b :: r > 0';
substitute on='let r := a :: let x := b :: r > 0';
substitute on='let x := b :: a > 0';
close on='a > 0';


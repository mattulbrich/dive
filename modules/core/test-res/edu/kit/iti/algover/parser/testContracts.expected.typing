(method testContracts
        ARGS
        (requires true[bool])
        (ensures true[bool])
        (ensures (== (old intfield[int])[int] intfield[int])[bool])
        (decreases 2[int])
        (BLOCK
         (while
             true[bool]
             (invariant false[bool])
             (decreases 2[int])
             BLOCK)))[scc_3]
